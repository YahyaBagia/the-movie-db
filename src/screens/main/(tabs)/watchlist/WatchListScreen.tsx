import { Text } from "react-native-paper";

import ScreenWrapper from "@/src/components/ScreenWrapper";

const WatchListScreen = () => {
  return (
    <ScreenWrapper>
      <Text variant="headlineLarge">WATCHLIST</Text>
    </ScreenWrapper>
  );
};

export default WatchListScreen;
